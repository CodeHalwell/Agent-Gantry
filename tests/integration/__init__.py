"""Integration tests for Agent-Gantry.

These tests require external services (vector stores, LLM APIs, etc.)
and are not run in CI by default. Use the 'integration' pytest marker:

    pytest -m integration
"""
