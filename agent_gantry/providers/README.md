# agent_gantry/providers

Clients that import or federate tools from external systems.

- `__init__.py`: Provider package exports.
- `a2a_client.py`: Discovers skills from remote A2A agents and converts them into tools.
